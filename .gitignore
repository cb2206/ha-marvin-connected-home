__pycache__/
*.py[cod]
.venv/
.DS_Store
# captures contain live bearer tokens
*.mitm
marvin-api-request-email.md
# capture output — raw flows hold live tokens; the redacted JSONL is
# best-effort, so promote lines into API.md deliberately rather than by default
captures/
